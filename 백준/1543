doc = input()
word = input()

idx = 0
count = 0

while idx <= len(doc) - len(word):
    if doc[idx:idx + len(word)] == word:
        count += 1
        idx += len(word)
    else:
        idx += 1

print(count)
